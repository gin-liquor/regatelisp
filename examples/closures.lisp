; fn による無名関数と関数適用
((fn (x) (+ x 1)) 5)
((fn (x y) (* (+ x y) 2)) 3 4)
((fn () 123))

;; ; クロージャ: make-adder が返す関数は x=10 を捕捉し続ける
;; (let ((make-adder
;;         (fn (x)
;;           (fn (y)
;;             (+ x y)))))
;;   ((make-adder 10) 5))

; 字句スコープ: f が参照する x は呼び出し位置の 100 ではなく定義位置の 10
(let ((x 10))
  (let ((f (fn (y) (+ x y))))
    (let ((x 100))
      (f 1))))

; 高階関数
((fn (f x) (f x))
 (fn (n) (* n n))
 6)
