; let による並列束縛とシャドーイング
(let ((x 10)
      (y 20))
  (+ x y))

(let ((x 1))
  (let ((x 2))
    x))

; 並列束縛の確認: 内側の y は同じ let 内の x ではなく外側の x を見る
(let ((x 10))
  (let ((x 1)
        (y x))
    y))

; 組み込み関数のシャドーイング
(let ((+ (fn (x y) (- x y))))
  (+ 10 3))
